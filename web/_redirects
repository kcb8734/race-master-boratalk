# ══════════════════════════════════════════════════════════════════════
#  Cloudflare Pages SPA 라우팅 — Flutter Web 단일 페이지 앱
#  Flutter Web은 모든 경로를 index.html로 리다이렉트해야 함
#  (새로고침 / 직접 URL 접근 시 404 방지)
# ══════════════════════════════════════════════════════════════════════

# SPA Fallback: 모든 404를 index.html로 서빙 (200 응답)
/*    /index.html    200
